CREATE TABLE [IF NOT EXISTS] [oltp].trade (
	timestamp TEXT PRIMARY KEY,
	action TEXT PRIMARY KEY,
	timestamp data_type PRIMARY KEY,
	timestamp data_type PRIMARY KEY,
  column_2 data_type NOT NULL,
	column_3 data_type DEFAULT 0,
	table_constraints
) [WITHOUT ROWID];